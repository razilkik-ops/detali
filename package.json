{
  "name": "spectehosnastka-site",
  "version": "1.0.0",
  "private": true,
  "description": "Многостраничный сайт ЧПУП СпецТехОснастка",
  "main": "server.js",
  "scripts": {
    "build:pages": "node scripts/build-pages.js",
    "dev": "node --watch server.js",
    "start": "node server.js",
    "check": "node --check server.js && node --check data/services.js && node --check data/company.js && node --check scripts/build-pages.js && node --check public/js/app.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@fontsource/manrope": "^5.2.7",
    "bootstrap-icons": "^1.13.1",
    "compression": "^1.8.1",
    "ejs": "^3.1.10",
    "express": "^5.1.0",
    "helmet": "^8.1.0"
  }
}
