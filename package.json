{
  "name": "spectehosnastka-site",
  "version": "1.0.0",
  "private": true,
  "description": "Многостраничный сайт ЧПУП СпецТехОснастка",
  "main": "server.js",
  "scripts": {
    "build:pages": "node scripts/build-pages.js && node scripts/seo-audit.js && node scripts/audit-public.js",
    "audit:seo": "node scripts/seo-audit.js",
    "audit:public": "node scripts/audit-public.js",
    "dev": "node --watch server.js",
    "start": "node server.js",
    "test": "node --test --test-concurrency=1",
    "check": "node --check server.js && node --check data/services.js && node --check data/company.js && node --check scripts/build-pages.js && node --check scripts/seo-audit.js && node --check scripts/audit-public.js && node --check public/js/app.js && npm test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@fontsource/manrope": "^5.2.7",
    "bootstrap-icons": "^1.13.1",
    "compression": "^1.8.1",
    "ejs": "^6.0.1",
    "express": "^5.1.0",
    "helmet": "^8.1.0"
  }
}
